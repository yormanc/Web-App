/* ===== ESTILOS RESPONSIVOS ===== */

/* TABLETS (hasta 768px de ancho) */
@media screen and (max-width: 768px) {
    
    /* Ajustar espacios para pantallas más pequeñas */
    :root {
        --espacioBase: 12px;
    }
    
    .contenedorPrincipal {
        padding: 0 10px;
    }
    
    /* Ajustar banner */
    .bannerGradiente {
        height: 150px;
    }
    
    /* Perfil en una columna */
    .perfilUsuario {
        grid-template-columns: 1fr;
        text-align: center;
        gap: 15px;
    }
    
    .fotoPerfil {
        width: 100px;
        height: 100px;
        margin: 0 auto;
    }
    
    .detallesContacto {
        justify-content: center;
        flex-direction: column;
        gap: 10px;
    }
    
    .botonEditarPerfil {
        width: 100%;
    }
    
    /* Rejilla de capacidades: 2 columnas */
    .rejillaCapacidades {
        grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
        gap: 12px;
    }
    
    .cabeceraCapacidades {
        flex-direction: column;
        align-items: flex-start;
    }
    
    .cabeceraCapacidades h2 {
        font-size: 20px;
    }
    
    .botonPrimario {
        width: 100%;
    }
    
    /* Ajustar modal */
    .contenidoModal {
        width: 95%;
        max-width: 100%;
        margin: 20px;
    }
    
    .cabeceraModal h2 {
        font-size: 18px;
    }
    
    .pieModal {
        flex-direction: column;
    }
    
    .pieModal button {
        width: 100%;
    }
    
    /* Reducir fuentes */
    .infoPerfil h1 {
        font-size: 22px;
    }
    
    .bioPerfil {
        font-size: 14px;
    }
    
    .detalle {
        font-size: 12px;
    }
}

/* MÓVILES PEQUEÑOS (hasta 480px de ancho) */
@media screen and (max-width: 480px) {
    
    :root {
        --espacioBase: 10px;
        --radioBorde: 6px;
    }
    
    .contenedorPrincipal {
        padding: 0 8px;
    }
    
    /* Banner más pequeño */
    .bannerGradiente {
        height: 120px;
        padding: 8px;
    }
    
    .botonEditarBanner {
        padding: 6px 12px;
        font-size: 11px;
    }
    
    /* Perfil comprimido */
    .perfilUsuario {
        padding: 12px 8px;
        gap: 12px;
    }
    
    .fotoPerfil {
        width: 80px;
        height: 80px;
    }
    
    .infoPerfil h1 {
        font-size: 18px;
        margin-bottom: 6px;
    }
    
    .bioPerfil {
        font-size: 13px;
        margin-bottom: 10px;
    }
    
    .detallesContacto {
        gap: 8px;
    }
    
    .detalle {
        font-size: 11px;
    }
    
    .botonEditarPerfil {
        padding: 8px 16px;
        font-size: 13px;
    }
    
    /* Sección de capacidades */
    .seccionCapacidades {
        padding: 10px;
        margin: 15px 0;
    }
    
    .cabeceraCapacidades {
        gap: 10px;
        margin-bottom: 12px;
    }
    
    .cabeceraCapacidades h2 {
        font-size: 18px;
    }
    
    /* Rejilla de una columna en móviles */
    .rejillaCapacidades {
        grid-template-columns: 1fr;
        gap: 10px;
    }
    
    .tarjetaCapacidad {
        padding: 12px;
    }
    
    .encabezadoTarjeta {
        flex-direction: column;
        align-items: flex-start;
        margin-bottom: 10px;
        gap: 8px;
    }
    
    .encabezadoTarjeta h3 {
        font-size: 16px;
    }
    
    .etiquetaNivel {
        padding: 3px 10px;
        font-size: 11px;
    }
    
    .accionesTarjeta {
        gap: 8px;
    }
    
    .botonChico {
        padding: 6px 10px;
        font-size: 11px;
    }
    
    /* Formularios */
    .formulario {
        gap: 12px;
    }
    
    .grupoFormulario label {
        font-size: 13px;
        margin-bottom: 6px;
    }
    
    .grupoFormulario input,
    .grupoFormulario textarea,
    .grupoFormulario select {
        padding: 8px;
        font-size: 13px;
    }
    
    .grupoFormulario textarea {
        min-height: 80px;
    }
    
    /* Modal */
    .modal {
        align-items: flex-end;
    }
    
    .contenidoModal {
        width: 100%;
        max-width: 100%;
        border-radius: 12px 12px 0 0;
        margin: 0;
    }
    
    .cabeceraModal {
        padding: 12px;
        gap: 10px;
    }
    
    .cabeceraModal h2 {
        font-size: 16px;
    }
    
    .botonCerrarModal {
        font-size: 24px;
    }
    
    .contenidoModal .formulario {
        padding: 12px;
        gap: 10px;
    }
    
    .pieModal {
        padding: 12px;
        gap: 8px;
        flex-direction: column;
    }
    
    .pieModal button {
        padding: 10px 16px;
        font-size: 13px;
        width: 100%;
    }
    
    /* Pie de página */
    .piePagina {
        padding: 12px;
        font-size: 12px;
        margin-top: 15px;
    }
    
    /* Mensaje vacío */
    .mensajeVacio {
        padding: 20px 10px;
        font-size: 14px;
    }
    
    /* Ocultar elementos no necesarios en móvil */
    .botonPrimario {
        padding: 8px 16px;
        font-size: 13px;
    }
}

/* ORIENTACIÓN HORIZONTAL (Landscape) */
@media screen and (max-height: 500px) and (orientation: landscape) {
    
    .bannerGradiente {
        height: 100px;
    }
    
    .perfilUsuario {
        grid-template-columns: auto 1fr auto;
        gap: 15px;
        padding: 10px;
    }
    
    .fotoPerfil {
        width: 80px;
        height: 80px;
    }
    
    .infoPerfil h1 {
        font-size: 18px;
    }
    
    .bioPerfil {
        display: none;
    }
    
    .detallesContacto {
        flex-direction: row;
        gap: 15px;
        font-size: 12px;
    }
}

/* IMPRESIÓN */
@media print {
    
    body {
        background: white;
    }
    
    .botonEditarBanner,
    .botonEditarPerfil,
    .botonPrimario,
    .botonChico,
    .modal,
    .accionesTarjeta {
        display: none !important;
    }
    
    .perfilUsuario {
        border: 1px solid #ccc;
        padding: 20px;
    }
    
    .seccionCapacidades {
        page-break-inside: avoid;
    }
    
    .tarjetaCapacidad {
        page-break-inside: avoid;
    }
}

/* PREFERENCIA DE COLORES OSCUROS */
@media (prefers-color-scheme: dark) {
    :root {
        --colorFondo: #1a1a1a;
        --colorTexto: #e0e0e0;
        --colorTextoClaro: #a0a0a0;
        --colorBorde: #333;
    }
    
    body {
        background-color: var(--colorFondo);
    }
    
    .seccionEncabezado,
    .seccionCapacidades,
    .contenidoModal {
        background: #252525;
        color: var(--colorTexto);
    }
    
    .tarjetaCapacidad {
        background: #2a2a2a;
    }
    
    .grupoFormulario input,
    .grupoFormulario textarea,
    .grupoFormulario select {
        background: #3a3a3a;
        color: var(--colorTexto);
        border-color: var(--colorBorde);
    }
    
    .piePagina {
        background: #2a2a2a;
    }
}
